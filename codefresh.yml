version: '1.0'
steps:
  test_fsharp_utilities:
    title: Test F# utilities
    image: mcr.microsoft.com/dotnet/core/sdk:2.1
    commands:
      - dotnet tool install fake-cli -g
      - fake build -t Test
