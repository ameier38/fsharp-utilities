version: '1.0'
steps:
  install_dependencies:
    title: Install dependencies
    image: ameier38/dotnet-mono-sdk:2.1
    commands:
      - fake build -t InstallDependencies
  run_tests:
    title: Run tests 
    image: fsharp
    commands:
      - test.fsx
